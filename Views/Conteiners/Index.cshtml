@model IEnumerable<Estaleiro.Models.Conteiner>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumeroConteiner)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoConteiner)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StatusConteiner)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategoriaConteiner)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroConteiner)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoConteiner)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StatusConteiner)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoriaConteiner)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
