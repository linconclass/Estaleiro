@model Estaleiro.Models.Conteiner
@using Estaleiro.Models.Enums

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Conteiner</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ID" />
            <div class="form-group">
                <label asp-for="Cliente" class="control-label"></label>
                <input asp-for="Cliente" class="form-control" />
                <span asp-validation-for="Cliente" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="NumeroConteiner" class="control-label"></label>
                <input asp-for="NumeroConteiner" class="form-control" />
                <span asp-validation-for="NumeroConteiner" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TipoConteiner" class="control-label"></label>
                <select asp-for="TipoConteiner" class="form-control" asp-items="@(new SelectList(Enum.GetValues(typeof(TipoConteiner))))"></select>
                <span asp-validation-for="TipoConteiner" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="StatusConteiner" class="control-label"></label>
                <select asp-for="StatusConteiner" class="form-control" asp-items="@(new SelectList(Enum.GetValues(typeof(StatusConteiner))))"></select>
                <span asp-validation-for="StatusConteiner" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CategoriaConteiner" class="control-label"></label>
                <select asp-for="CategoriaConteiner" class="form-control" asp-items="@(new SelectList(Enum.GetValues(typeof(CategoriaConteiner))))"></select>
                <span asp-validation-for="CategoriaConteiner" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
